import ReactOnRails from 'react-on-rails';

import MainMenu from '../bundles/MainMenu/components/MainMenu';

// This is how react_on_rails can see the MainMenu in the browser.
ReactOnRails.register({
  MainMenu,
});
